[package]
name = "tokio-async-demo"
version = "0.1.0"
edition = "2021"  # 确保使用 2021 及以上版本（async/await 稳定支持）

[dependencies]
# Tokio 核心依赖：full 特性包含所有常用功能（网络、文件、定时器等）
# features = ["full"] 适合快速开发，生产环境可按需裁剪（如 "rt-multi-thread", "net", "time"）
tokio = { version = "1.0", features = ["full"] }